{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "pnpm --filter @workspace/fraud-detection-dashboard run dev",
    "build": "pnpm --filter @workspace/fraud-detection-dashboard run build",
    "typecheck": "pnpm --filter @workspace/fraud-detection-dashboard run typecheck"
  },
  "private": true,
  "dependencies": {},
  "devDependencies": {
    "prettier": "^3.9.6",
    "typescript": "~5.9.3"
  }
}
