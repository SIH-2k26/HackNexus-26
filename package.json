{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "cd artifacts/fraud-detection-dashboard && npm run dev",
    "build": "cd artifacts/fraud-detection-dashboard && npm install && npm run build && cp -r dist ../../dist",
    "typecheck": "cd artifacts/fraud-detection-dashboard && npm run typecheck"
  },
  "private": true
}
