{
  "buildCommand": "cd artifacts/fraud-detection-dashboard && npm install && npm run build",
  "outputDirectory": "artifacts/fraud-detection-dashboard/dist/public",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
