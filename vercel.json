{
  "buildCommand": "cd artifacts/fraud-detection-dashboard && npm install && npm run build && cp -r dist ../../dist",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
